import { cabinetMetadata } from '@/lib/seo'

// Kabinet sahifasi — qidiruvda indekslanmaydi, faqat to'g'ri <title> beradi.
export const metadata = cabinetMetadata('Профиль заказчика')

export default function AdminClientDetailLayout({ children }) {
    return children
}
