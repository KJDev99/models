# Отчёт фронтенда №11

30.08.2026 · API `https://admin.modelworkrf.ru/api/v1`

Фронт переключён на `admin.modelworkrf.ru`. Всё ниже проверено именно на нём.

**Закрыто, вопросов нет:**

- `shoots_count` / `cases_count` в списке исполнителей — приходят, чипы рисуются;
- `agency` в анкете исполнителя — приходит, плашка со ссылкой работает;
- `GET /site/support` — 200 у всех ролей, диалог открывается, ответ админа доходит;
- `experience_min` / `experience_max` — работают, фильтр «Опыт» собран правильно.
