import { cabinetMetadata } from '@/lib/seo'

// Kabinet sahifasi — qidiruvda indekslanmaydi, faqat to'g'ri <title> beradi.
export const metadata = cabinetMetadata('Изменить пароль')

export default function AgencyPasswordViewLayout({ children }) {
    return children
}
