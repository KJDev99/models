import { cabinetMetadata } from '@/lib/seo'

export const metadata = cabinetMetadata('Добавить исполнителя')

export default function AdminAgencyNewExecutorLayout({ children }) {
    return children
}
