# Отчёт фронтенда №8

22.08.2026 · API `https://admin.agunastroy.ru/api/v1`

Отчёт №7 закрыт. Проверили живыми запросами и в браузере:

- `?status=hidden` → `200` на `/agency/performers` и `/customer/venues`,
  вкладка «Скрытые» на обоих экранах работает;
- `?is_hidden=true|false` реально фильтрует, поле `is_hidden` появилось
  в элементе `/customer/venues`;
- неизвестный `status` → `422` со списком допустимых значений вместо `500`;
- демо-фото перезалиты: было 29 градиентов из 70, осталось 4;
  `p-vid7.jpg` больше не 128×128.

Полный цикл скрытия проверен: исполнитель скрыл анкету → появился во вкладке
«Скрытые» у агентства, пропал из «Активных» и из публичного каталога
(15 → 14) → вернул видимость → всё откатилось.

Блокеров нет. Осталась мелочь по демо-данным.

---

## Демо-фото: 4 градиента и дубли

Четыре карточки в каталоге по-прежнему сплошная заливка вместо фотографии:

| Карточка | Файл | Вес |
|---|---|---|
| Артём Смирнов (модель) | `p-mdl14.jpg` | 28 025 б |
| Юлия Медведева (видеограф) | `p-vid3.jpg` | 26 969 б |
| агентство | `p-ag2.jpg` | 25 466 б |
| агентство | `p-ag6.jpg` | 24 434 б |

Ещё две обложки лёгкие и, похоже, тоже заливка: `g-customer.jpg` (33 887 б),
`g-prj7.jpg` (36 570 б). Для сравнения — настоящее фото `800×1000` весит
70–170 КБ.

Отдельно: **16 групп дублей** — один файл под разными именами, поэтому в
каталоге одно лицо идёт подряд несколько раз:

```
p-mdl4 = p-mdl7 = p-mdl13 = p-mdl15 = p-ph4     (5 карточек — одно фото)
p-mdl10 = p-mdl11 = p-mdl12 = p-vid5            (4 карточки)
p-mdl3 = p-photo = p-vid4 = p-video             (4 карточки)
g-prj9 = g-project = g-ven12                    (3 карточки)
```

Не блокер, сайт работает. Просто на демо каталог выглядит однообразно.

---

Проверено под всеми семью учётками (заказчик, компания, модель, фотограф,
видеограф, агентство, админ): все экраны открываются, ошибок API, JS и
консоли нет нигде. Сборка проходит (107/107), линтер чистый.
